# Auto-generated via `dist generate`. Builds artifacts, publishes to GitHub Releases.
# License: MIT OR Apache-2.0


name: Release
permissions: {}
concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: true

# Triggered by tags matching Cargo SemVer (e.g. v1.0.0, v0.1.0-prerelease.1).
# Tag: [PACKAGE_NAME/]VERSION. Prerelease suffix → GitHub prerelease.
# Max 3 concurrent tag-triggered runs per commit.

on:
  pull_request:
  push:
    tags:
      - '**[0-9]+.[0-9]+.[0-9]+*'
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Build and upload artifacts without creating a release'
        type: boolean
        default: false

jobs:
  # Run 'dist plan' (or host) to determine what tasks we need to do
  plan:
    name: Plan
    runs-on: "ubuntu-22.04"
    outputs:
      val: ${{ steps.plan.outputs.manifest }}
      tag: ${{ !github.event.pull_request && github.ref_name || '' }}
      tag-flag: ${{ !github.event.pull_request && format('--tag={0}', github.ref_name) || '' }}
      publishing: ${{ !github.event.pull_request }}
      dry_run: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.dry_run == 'true' }}
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Install dist
        # bash for pipefail — sh silently ignores failing curl in a pipe
        shell: bash
        run: "curl --proto '=https' --tlsv1.2 -LsSf https://github.com/axodotdev/cargo-dist/releases/download/v0.32.0/cargo-dist-installer.sh | sh"
      - name: Cache dist
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: cargo-dist-cache
          path: ~/.cargo/bin/dist
      # Fork PRs lack target-repo secrets — on push: host (create release), on PR: plan (no secrets).
      - id: plan
        env:
          DIST_CMD: ${{ (!github.event.pull_request && format('host --steps=create --tag={0}', github.ref_name)) || 'plan' }}
        run: |
          dist $DIST_CMD --output-format=json > plan-dist-manifest.json
          echo "dist ran successfully"
          cat plan-dist-manifest.json
          echo "manifest=$(jq -c "." plan-dist-manifest.json)" >> "$GITHUB_OUTPUT"
      - name: "Upload dist-manifest.json"
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: artifacts-plan-dist-manifest
          path: plan-dist-manifest.json

  # Build and packages all the platform-specific things
  build-local-artifacts:
    name: build-local-artifacts (${{ join(matrix.targets, ', ') }})
    # Let the initial task tell us to not run (currently very blunt)
    needs:
      - plan
    if: ${{ fromJson(needs.plan.outputs.val).ci.github.artifacts_matrix.include != null && (needs.plan.outputs.publishing == 'true' || fromJson(needs.plan.outputs.val).ci.github.pr_run_mode == 'upload') }}
    strategy:
      fail-fast: false
      # Matrix computed by dist: 1 global task (universal installers) + N local tasks (per-platform binaries).
      matrix: ${{ fromJson(needs.plan.outputs.val).ci.github.artifacts_matrix }}
    runs-on: ${{ matrix.runner }}
    container: ${{ matrix.container && matrix.container.image || null }}
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      BUILD_MANIFEST_NAME: target/distrib/${{ join(matrix.targets, '-') }}-dist-manifest.json
    steps:
      - name: enable windows longpaths
        run: |
          git config --global core.longpaths true
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Install Rust non-interactively if not already installed
        if: ${{ matrix.container }}
        run: |
          if ! command -v cargo > /dev/null 2>&1; then
            curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
            echo "$HOME/.cargo/bin" >> $GITHUB_PATH
          fi
      - name: Install dist
        run: ${{ matrix.install_dist.run }}
      - name: Fetch local artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          pattern: artifacts-*
          path: target/distrib/
          merge-multiple: true
      - name: Install dependencies
        run: |
          ${{ matrix.packages_install }}
      - name: Build artifacts
        env:
          TAG_FLAG: ${{ needs.plan.outputs.tag-flag }}
          DIST_ARGS: ${{ matrix.dist_args }}
        run: |
          dist build $TAG_FLAG --print=linkage --output-format=json $DIST_ARGS > dist-manifest.json
          echo "dist ran successfully"
      - id: cargo-dist
        name: Post-build
        # bash for consistent GITHUB_OUTPUT syntax (powershell diverges)
        shell: bash
        run: |
          # Parse out what we just built and upload it to scratch storage
          echo "paths<<EOF" >> "$GITHUB_OUTPUT"
          dist print-upload-files-from-manifest --manifest dist-manifest.json >> "$GITHUB_OUTPUT"
          echo "EOF" >> "$GITHUB_OUTPUT"

          cp dist-manifest.json "$BUILD_MANIFEST_NAME"
      - name: "Upload artifacts"
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: artifacts-build-local-${{ join(matrix.targets, '_') }}
          path: |
            ${{ steps.cargo-dist.outputs.paths }}
            ${{ env.BUILD_MANIFEST_NAME }}

  # Build and package all the platform-agnostic(ish) things
  build-global-artifacts:
    name: Build global artifacts
    needs:
      - plan
      - build-local-artifacts
    runs-on: "ubuntu-22.04"
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      BUILD_MANIFEST_NAME: target/distrib/global-dist-manifest.json
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Install cached dist
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          name: cargo-dist-cache
          path: ~/.cargo/bin/
      - run: chmod +x ~/.cargo/bin/dist
      - name: Fetch local artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          pattern: artifacts-*
          path: target/distrib/
          merge-multiple: true
      - id: cargo-dist
        shell: bash
        env:
          TAG_FLAG: ${{ needs.plan.outputs.tag-flag }}
        run: |
          dist build $TAG_FLAG --output-format=json "--artifacts=global" > dist-manifest.json
          echo "dist ran successfully"

          # Parse out what we just built and upload it to scratch storage
          echo "paths<<EOF" >> "$GITHUB_OUTPUT"
          jq --raw-output ".upload_files[]" dist-manifest.json >> "$GITHUB_OUTPUT"
          echo "EOF" >> "$GITHUB_OUTPUT"

          cp dist-manifest.json "$BUILD_MANIFEST_NAME"
      - name: "Upload artifacts"
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: artifacts-build-global
          path: |
            ${{ steps.cargo-dist.outputs.paths }}
            ${{ env.BUILD_MANIFEST_NAME }}
  # Determines if we should publish/announce
  host:
    name: Host
    permissions:
      contents: write     # release creation + asset upload
      attestations: write  # SLSA provenance for release artifacts
      id-token: write     # OIDC for attestation signing
    needs:
      - plan
      - build-local-artifacts
      - build-global-artifacts
    # Only run if we're "publishing", and only if plan, local and global didn't fail (skipped is fine)
    if: ${{ always() && needs.plan.result == 'success' && (needs.plan.outputs.publishing == 'true' || needs.plan.outputs.dry_run == 'true') && (needs.build-global-artifacts.result == 'skipped' || needs.build-global-artifacts.result == 'success') && (needs.build-local-artifacts.result == 'skipped' || needs.build-local-artifacts.result == 'success') }}
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    runs-on: "ubuntu-22.04"
    outputs:
      val: ${{ steps.host.outputs.manifest }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
      - name: Install cached dist
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          name: cargo-dist-cache
          path: ~/.cargo/bin/
      - run: chmod +x ~/.cargo/bin/dist
      # Fetch artifacts from scratch-storage
      - name: Fetch artifacts
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          pattern: artifacts-*
          path: target/distrib/
          merge-multiple: true
      - id: host
        shell: bash
        env:
          DRY_RUN: ${{ needs.plan.outputs.dry_run }}
          TAG_FLAG: ${{ needs.plan.outputs.tag-flag }}
        run: |
          STEPS="--steps=upload"
          if [ "$DRY_RUN" != "true" ]; then
            STEPS="$STEPS --steps=release"
          fi
          dist host $TAG_FLAG $STEPS --output-format=json > dist-manifest.json
          if [ "$DRY_RUN" = "true" ]; then
            echo "artifacts uploaded (dry run — no release created)"
          else
            echo "artifacts uploaded and released successfully"
          fi
          cat dist-manifest.json
          echo "manifest=$(jq -c "." dist-manifest.json)" >> "$GITHUB_OUTPUT"
      - name: "Upload dist-manifest.json"
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: artifacts-dist-manifest
          path: dist-manifest.json
      - name: "Download GitHub Artifacts"
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          pattern: artifacts-*
          path: artifacts
          merge-multiple: true
      - name: Cleanup
        run: |
          rm -f artifacts/*-dist-manifest.json
      - name: Generate artifact attestations
        if: ${{ needs.plan.outputs.dry_run != 'true' }}
        # Attestation is defense-in-depth — transient service outage must not block a release.
        continue-on-error: true
        uses: actions/attest-build-provenance@0f67c3f4856b2e3261c31976d6725780e5e4c373 # v4.1.1
        with:
          subject-path: "artifacts/claudebar-*.tar.gz"
      # GITHUB_TOKEN releases don't trigger downstream workflows (GitHub loop-prevention).
      # Use App token so verify-install.yml's `on: release: published` fires.
      # ⚠️ Reapply this step after `dist generate`.
      - uses: actions/create-github-app-token@fee1f7d63c2ff003460e3d139729b119787bc349 # v2
        if: ${{ needs.plan.outputs.dry_run != 'true' }}
        id: release-app-token
        with:
          app-id: ${{ secrets.RENOVATE_APP_ID }}
          private-key: ${{ secrets.RENOVATE_PRIVATE_KEY }}
          owner: "micschr0"
          repositories: "claudebar"
          permission-contents: write
      - name: Create GitHub Release
        if: ${{ needs.plan.outputs.dry_run != 'true' }}
        env:
          GH_TOKEN: ${{ steps.release-app-token.outputs.token }}
          PRERELEASE_FLAG: "${{ fromJson(steps.host.outputs.manifest).announcement_is_prerelease && '--prerelease' || '' }}"
          ANNOUNCEMENT_TITLE: "${{ fromJson(steps.host.outputs.manifest).announcement_title }}"
          ANNOUNCEMENT_BODY: "${{ fromJson(steps.host.outputs.manifest).announcement_github_body }}"
          RELEASE_COMMIT: "${{ github.sha }}"
          RELEASE_TAG: ${{ needs.plan.outputs.tag }}
        run: |
          # Write and read notes from a file to avoid quoting breaking things
          echo "$ANNOUNCEMENT_BODY" > $RUNNER_TEMP/notes.txt

          gh release create "$RELEASE_TAG" --target "$RELEASE_COMMIT" $PRERELEASE_FLAG --title "$ANNOUNCEMENT_TITLE" --notes-file "$RUNNER_TEMP/notes.txt" artifacts/*
      - name: Prune older prereleases
        # Tolerate grep no-match + 404s so host can't fail and block homebrew/announce.
        if: ${{ needs.plan.outputs.dry_run != 'true' }}
        shell: bash
        env:
          GH_TOKEN: ${{ steps.release-app-token.outputs.token }}
          RELEASE_TAG: ${{ needs.plan.outputs.tag }}
        run: |
          gh release list --json tagName,isPrerelease -q \
            '.[] | select(.isPrerelease) | .tagName' \
            | { grep -vFx "$RELEASE_TAG" || true; } \
            | while IFS= read -r tag; do
                echo "pruning older prerelease: $tag"
                gh release delete --yes --cleanup-tag "$tag" || true
              done

  # Stable → claudebar.rb, prerelease → claudebar@beta.rb. Never in same commit → brew upgrade can't bounce stable users to beta. ⚠️ Reapply after 'dist generate'.

  custom-homebrew-app-token:
    name: Publish homebrew tap
    needs:
      - plan
      - host
    if: ${{ (!fromJson(needs.plan.outputs.val).announcement_is_prerelease || fromJson(needs.plan.outputs.val).publish_prereleases) && needs.plan.outputs.dry_run != 'true' }}
    runs-on: "ubuntu-22.04"
    permissions:
      contents: read  # Only needs to push to homebrew-tap via app token
    env:
      PLAN: ${{ needs.plan.outputs.val }}
      GITHUB_USER: "axo bot"
      GITHUB_EMAIL: "admin+bot@axo.dev"
    steps:
      - uses: actions/create-github-app-token@fee1f7d63c2ff003460e3d139729b119787bc349 # v2
        id: app-token
        with:
          app-id: ${{ secrets.HOMEBREW_TAP_APP_ID }}
          private-key: ${{ secrets.HOMEBREW_TAP_PRIVATE_KEY }}
          owner: "micschr0"
          repositories: "homebrew-tap"
          permission-contents: write
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: true
          repository: "micschr0/homebrew-tap"
          token: ${{ steps.app-token.outputs.token }}
      - name: Fetch homebrew formulae
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          pattern: artifacts-*
          path: Formula/
          merge-multiple: true
      - name: Commit formula files
        # Why: set -euo pipefail aborts on silent failures. Artifact asserted non-empty
        # before rewrite (partial download can't clobber). Prerelease: python strips
        # bottle block + renames class with verified subn count. brew audit || true surfaces
        # class/name drift without blocking.
        run: |
          set -euo pipefail

          git config --global user.name "${GITHUB_USER}"
          git config --global user.email "${GITHUB_EMAIL}"

          is_pre=$(echo "$PLAN" | jq --raw-output '.announcement_is_prerelease')
          for release in $(echo "$PLAN" | jq --compact-output '.releases[] | select([.artifacts[] | endswith(".rb")] | any)'); do
            filename=$(echo "$release" | jq '.artifacts[] | select(endswith(".rb"))' --raw-output)
            name=$(echo "$filename" | sed "s/\.rb$//")
            version=$(echo "$release" | jq .app_version --raw-output)
            # Refuse to write a half-clobbered formula if dist's
            # download-artifact step delivered nothing.
            if [ ! -s "$src" ]; then
              echo "::error::dist formula artifact missing or empty: $src" >&2
              exit 1
            fi

            if [ "$is_pre" = "true" ]; then
              target="Formula/${name}@beta.rb"
              commit_name="${name}@beta"
              python3 - "$src" "$target" <<'PY'
          # Strip the bottle block (multiline regex) and rename the class
          # with a verified substitution count — a no-op rename surfaces
          # dist template drift instead of pushing a broken formula.
          out = re.sub(
              r'^  bottle do\b.*?\n  end\n',
              '  bottle :disable, reason: "pre-release tap formula"\n',
              src_text,
              count=1,
              flags=re.MULTILINE | re.DOTALL,
          )
          out, n = re.subn(r'^class Claudebar ', 'class ClaudebarATBeta ', out, count=1, flags=re.MULTILINE)
          if n != 1:
              sys.stderr.write('::error::class rename failed — dist formula template drifted\n')
              sys.exit(1)
          open(sys.argv[2], 'w').write(out)
          PY
            else
              target="$src"
              commit_name="${name}"
            fi

            export PATH="/home/linuxbrew/.linuxbrew/bin:$PATH"
            brew update || true  # transient tap-remote flake must not abort publish
            brew style --except-cops FormulaAudit/Homepage,FormulaAudit/Desc,FormulaAuditStrict --fix "$target" || true
            brew audit --new --formula "$target" || true  # informational — must not block
            git add "$target"
            # Byte-identical (no-op) diff is benign
            if ! git diff --cached --quiet -- "$target"; then
              git commit -m "${commit_name} ${version}"
            fi
          done
          git push

  announce:
    name: Announce
    needs:
      - plan
      - host
      - custom-homebrew-app-token
    # always() lets prerelease publish skip while still waiting for host (required).
    if: ${{ always() && needs.host.result == 'success' && (needs.custom-homebrew-app-token.result == 'skipped' || needs.custom-homebrew-app-token.result == 'success') }}
    runs-on: "ubuntu-22.04"
    env:
      GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false
