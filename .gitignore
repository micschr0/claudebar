# Local-only Claude Code project context — intentionally kept out of the
# public repo. Claude Code still reads it from disk on every session.
# GitLab mirror CI — deployment infra, not part of the public repo
.gitlab-ci.yml
CLAUDE.md

# OS / editor cruft
.DS_Store

# Backup files created by the install skill
*.backup

# Python cache from gen_screenshots.py
__pycache__/
*.pyc

# Generated video output
video/out/
# CI benchmark artifact
benchmark-report.txt

# Rust build output
/target

# insta snapshot review leftovers
*.snap.new

# Agent worktrees
.claude/

# CodeGraph index (local-only)
.codegraph/
.mcp.json
*.swp
*.swo
*~
.idea/
.vscode/
*.code-workspace
.env
.env.*
!.env.example
node_modules/
.next/
dist/
build/
.venv/
venv/
vendor/
*.log
coverage/
.cache/
tmp/
.agents/

# Local skills tooling (not part of the project)
skills-lock.json
.agents/skills/