# Files

- [Testing strategy](overview.md) - Map the layered test suite for claudebar: Rust unit tests in each module, insta golden snapshots over fixtures, integration dispatch tests, bats suites for install.sh and the shell statusline, and the CI jobs (rust.yml, verify-install.yml, security.yml) that run and enforce them.
